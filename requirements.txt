# LLM client (OpenAI-compatible — works with Ollama and Big Pickle)
openai>=1.30.0

# Diffusion stack (workstation)
# Pin torch in the install command, not here, so the right CUDA wheel resolves
torch>=2.1.0
diffusers>=0.27.0,<0.32.0
# transformers >=4.50 references torch.float8_e8m0fnu which requires torch >=2.6;
# pinning lets older CUDA wheels (e.g. cu124 torch 2.5) keep working.
transformers>=4.40.0,<4.50.0
accelerate>=0.27.0
safetensors>=0.4.0

# Image + PDF
Pillow>=10.0.0
reportlab>=4.0.0

# Backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Frontend
gradio>=4.20.0

# Config + utils
tomli>=2.0.1; python_version < "3.11"
python-dotenv>=1.0.0
tqdm>=4.65.0
httpx>=0.27.0
