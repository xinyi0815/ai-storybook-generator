# LLM client (OpenAI-compatible — works with Ollama and Big Pickle)
openai>=1.30.0

# Diffusion stack (workstation)
# Pin torch in the install command, not here, so the right CUDA wheel resolves
torch>=2.1.0
# Pinned combo: latest diffusers (>=0.32) needs transformers 5.x; transformers
# 5.x needs torch.float8_e8m0fnu (torch >=2.6). Locking to a known-good 2024-era
# combo keeps cu124 / torch 2.5 wheels working out of the box.
diffusers==0.31.0
transformers==4.46.3
accelerate>=0.27.0
safetensors>=0.4.0

# Image + PDF
Pillow>=10.0.0
reportlab>=4.0.0

# Backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Frontend
gradio>=4.20.0

# Config + utils
tomli>=2.0.1; python_version < "3.11"
python-dotenv>=1.0.0
tqdm>=4.65.0
httpx>=0.27.0
